name: familring
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path: ^1.8.0
  http: ^0.13.3
  cupertino_icons: ^1.0.2
  table_calendar: ^3.0.5
  shared_preferences: ^2.0.6
  image_picker: ^1.1.2 #갤러리에서 이미지 가져오기
  permission_handler: ^10.2.0 #권한 요청
  path_provider: ^2.0.11
  flutter_image_compress: ^1.1.0 #이미지 크기 줄이기


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - images/photo_album_icon.png
    - images/question_list_icon.png
    - images/home_icon.png
    - images/calender_icon.png
    - images/mypage_icon.png
    - images/question.png
    - images/mypage.png
